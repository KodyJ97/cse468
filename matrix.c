#include <stdio.h> 
#include <stdlib.h> 

int main()
{
	printf("matrix.c");
	
}


/*
void* mygemm(void* arg)
{
	// Matrix Multiplication
	for(int i = 0; i < N; i++)
		for(int j = 0; j < N; j++)
			for(int k = 0; k < N; k++)
				matC[i][j] += matA[i][k] * matB[k][j];
}
*/
